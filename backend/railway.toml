[build]
builder = "nixpacks"
buildCommand = "if [ -d ../shared ]; then cd ../shared && npm install && npm run build && cd ../backend; fi && npm install && npm run build"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/health"
restartPolicyType = "on_failure"

